# Plow-Float-Mode-Detector
Arduino Nano project that monitors a Western plow controller’s RS-485 data stream, detects float mode from observed byte-pattern transitions, and drives an output light when float is active. Built through real hardware testing, signal logging, and iterative tuning under noisy transition conditions.
